apiVersion: v1
kind: Secret
metadata:
  name: example-app-secrets
  namespace: example-app
type: Opaque
# Override with real values:
#   kubectl create secret generic example-app-secrets -n example-app \
#     --from-literal=MY_KEY=value --dry-run=client -o yaml | kubectl apply -f -
stringData: {}
